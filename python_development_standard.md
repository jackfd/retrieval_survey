# 工业级 Python 开发规范

## 1. 文档目标与适用范围

本规范用于指导工业级 Python 项目的设计、开发、评审、测试与交付，目标是提升代码一致性、可维护性、可测试性、可观测性与工程可控性。

本规范适用于以下类型项目：

- 轻量脚本类项目
- 公共库 / SDK 类项目
- 服务 / 系统类项目
- 数据 / 算法类项目

本规范默认适用于团队协作开发场景，不适用于一次性临时实验代码或纯个人探索性原型。若项目已进入团队协作、重复执行、上线运行或持续迭代阶段，应遵循本规范。


## 2. 代码风格与静态检查

### 2.1 基本要求

1. 代码风格必须遵循 PEP 8。
2. 文档字符串必须遵循 PEP 257。
3. 公共接口必须提供类型标注。
4. 模块、类、函数、变量命名必须语义清晰，禁止使用含义模糊的缩写。
5. 代码应优先追求可读性，禁止为了简短而牺牲可理解性。

### 2.2 命名规范

1. 包名、模块名使用 `snake_case`。
2. 函数名、变量名使用 `snake_case`。
3. 类名使用 `PascalCase`。
4. 常量名使用 `UPPER_SNAKE_CASE`。
5. 私有实现使用单下划线前缀。
6. 禁止使用与上下文无关的命名，如 `tmp`、`data2`、`foo`、`do_stuff`，除非为极小作用域的临时变量且语义明确。

### 2.3 结构复杂度控制

1. 函数必须保持单一职责。
2. 单个函数不应承载输入校验、核心处理、异常转换、日志拼装、结果格式化等多类职责。
3. 超长函数必须拆分。
4. 超大类必须按职责拆分。
5. 超大文件必须按模块边界拆分。
6. 禁止使用深层嵌套控制流，应优先通过卫语句、职责下沉、显式函数拆分降低复杂度。

### 2.4 注释与文档字符串

1. 注释用于说明“为什么”，而不是重复描述“做了什么”。
2. 公共函数、类、模块必须包含文档字符串。
3. 文档字符串至少应说明用途、核心参数、返回值、异常边界。
4. 复杂业务规则、边界约束、兼容性原因必须写明。
5. 禁止保留过期注释、误导性注释和与代码不一致的说明。

### 2.5 导入规范

1. 导入顺序必须分组明确：标准库、第三方库、本地模块。
2. 禁止使用 `from xxx import *`。
3. 禁止循环依赖。
4. 禁止跨层直接引用其他模块的内部实现。
5. 模块间引用必须通过稳定公开接口完成。

### 2.6 类型标注

1. 公共函数、类方法、模块级导出对象必须有类型标注。
2. 核心内部函数建议补齐类型标注。
3. 复杂结构优先使用 `dataclass`、`TypedDict`、`Protocol`、`Enum` 等表达，而不是依赖隐式约定。
4. 禁止将 `Any` 作为逃避建模的默认手段。
5. 对外契约涉及的可空值、集合元素类型、返回结构必须明确。

### 2.7 静态检查工具链

推荐标准工具链如下：

- 格式化：`black`
- import 排序：`isort`
- 代码检查：`ruff`
- 类型检查：`mypy`
- 安全扫描：`bandit` 

### 2.8 CI 门禁要求

持续集成流程至少应包含以下门禁：

1. 格式化检查通过
2. lint 检查通过
3. 类型检查通过
4. 自动化测试通过

未通过上述门禁的变更不得合并至主分支。

---

## 3. 模块设计与接口契约

### 3.1 模块设计原则

1. 模块必须职责单一、边界清晰。
2. 模块之间必须通过显式接口协作。
3. 禁止通过全局变量、隐式状态或猴子补丁实现跨模块通信。
4. 模块依赖方向必须清晰，避免双向依赖与隐式耦合。
5. 模块划分应优先服务于可维护性与可替换性，而不是形式上的分层完整。

### 3.2 接口契约要求

每个对外接口必须明确以下内容：

1. 输入参数类型与约束
2. 返回值类型与结构
3. 可能抛出的异常类型
4. 副作用边界
5. 幂等性要求（如适用）

### 3.3 数据模型规范

1. 共享数据结构必须标准化。
2. 简单结构优先使用 `dataclass`。
3. 枚举值必须使用 `Enum` 统一表达，禁止散落硬编码字符串。
4. 输入输出结构不得依赖调用方“默认知道”的隐式字段。
5. 数据模型变更必须评估兼容性影响。

### 3.4 抽象层使用原则

1. 只有在存在多实现、明确扩展点或稳定替换需求时，才引入抽象层。
2. 优先使用组合而不是继承。
3. 继承仅用于明确的类型层次关系。
4. 禁止为“看起来更高级”而增加无业务价值的抽象层。
5. 禁止滥用设计模式。

### 3.5 依赖注入与外部依赖隔离

1. 外部依赖必须通过构造参数、函数参数或明确工厂注入。
2. 禁止在核心业务逻辑中硬编码数据库连接、HTTP 客户端、文件路径、环境变量读取逻辑。
3. 核心逻辑必须可以在不依赖真实外部资源的条件下独立测试。
4. 第三方能力必须封装为可替换边界。

### 3.6 包与目录组织

1. 目录结构必须体现模块边界。
2. 不同职责代码必须分目录管理，例如：
   - `domain`
   - `application`
   - `infra`
   - `interfaces`
   - `tests`
3. 临时代码、实验脚本、生产代码不得混放。
4. 公共工具代码不得无限制堆积为“万能 utils”。

---

## 4. 异常与错误码

### 4.1 基本原则

1. 项目必须定义统一异常基类。
2. 异常体系必须表达清晰语义，而不是仅区分“成功 / 失败”。
3. 错误处理目标是可定位、可分层处理、可监控，而不是简单中断流程。

### 4.2 异常分类建议

项目应根据业务场景定义有限且稳定的异常类别，通常包括：

- 配置错误
- 输入参数错误
- 数据校验错误
- 状态冲突错误
- 外部依赖错误
- 权限或访问错误
- 未预期系统错误（兜底）

### 4.3 异常使用规范

1. 底层应尽量抛出语义化异常。
2. 中间层仅在以下场景捕获异常：补充业务上下文、将底层技术异常转换为稳定领域异常、在系统边界处将异常映射为外部响应或运行控制行为。无上述必要时，应让异常自然上抛。
3. 顶层统一收敛异常，并映射为退出码、任务状态、HTTP 响应或错误输出。
4. 禁止吞异常。
5. 禁止使用无边界的裸 `except:`。
6. 禁止 `except Exception:` 后静默返回默认值。

### 4.4 错误码规范

1. 错误码必须标准化、可检索、可统计。
2. 错误码建议采用分层命名，如：
   - `config.missing`
   - `input.invalid`
   - `db.connection_failed`
   - `external.timeout`
3. 错误码应与异常类型建立稳定映射关系。
4. 同一错误场景不得使用多个含义重叠的错误码。
5. 错误码必须由项目统一管理，不得在各处随意定义。

### 4.5 异常传播边界

1. 资源清理由资源所属层负责。
2. 事务回滚由事务边界负责。
3. 重试只能在具备幂等保障和策略判断能力的一层执行。
4. 补偿逻辑由业务编排层控制，不作为底层默认职责。
5. 异常转换必须保留根因上下文，避免丢失可定位信息。

### 4.6 退出码与外部输出

对于可执行程序、脚本、批任务、CLI 工具，必须定义统一退出码策略：

1. 成功退出码为 0
2. 不同类型失败应映射为有限个稳定退出码
3. 退出码必须可文档化、可用于自动化调度与监控系统识别

---

## 5. 日志与可观测性

### 5.1 基本原则

1. 日志用于记录运行事实，不用于替代业务状态存储。
2. 可观测性必须覆盖日志、指标、链路追踪三个层面中的适用部分。
3. 日志记录必须服务于定位问题、还原上下文、支撑监控。

### 5.2 日志规范

1. 统一使用 `logging` 或项目统一封装的日志组件。
2. logging 输出必须使用参数化格式化，例如 `logger.error("x=%s", x)`。
3. 关键上下文字段直接写入日志消息；除非项目已统一采用结构化日志体系，否则不要求使用 extra。
4. 每个错误应在首次可定位位置记录一次完整日志。
5. 禁止在多个层级重复打印同一异常，造成日志污染。

### 5.3 日志级别定义

- `DEBUG`：调试细节，仅用于开发和定位问题
- `INFO`：关键流程节点、状态变化、重要执行结果
- `WARNING`：可恢复问题、降级、重试、边界异常
- `ERROR`：当前操作失败
- `CRITICAL`：系统级不可恢复故障

### 5.4 日志内容要求

关键日志应包含以下信息中的适用部分：

1. 事件名称
2. 函数位置
3. 业务主键 / 请求标识 / 任务标识
4. 错误码
5. 关键输入参数摘要
6. 外部依赖信息
7. 必要时的堆栈信息



### 5.6 敏感信息治理

1. 日志中不得输出密码、令牌、密钥、身份证号、银行卡号等敏感信息。
2. 对必要的用户标识、账号标识应进行脱敏或摘要处理。
3. 错误上下文记录必须遵循最小必要原则。

### 5.7 指标与监控

关键路径必须具备可监控指标，通常包括：

- 调用次数
- 成功率
- 失败率
- 延迟分布
- 重试次数
- 外部依赖超时率
- 队列积压或任务处理时延

### 5.8 Trace 与链路追踪

在服务型系统或跨模块复杂系统中，应建立统一链路追踪机制：

1. 上下游调用必须传递链路标识
2. 外部依赖调用必须纳入链路追踪范围
3. Trace 用于性能瓶颈定位与依赖分析，不替代日志与指标

---

## 6. 测试规范

### 6.1 基本原则

1. 测试是交付要求的一部分，不是可选补充。
2. 测试目标是验证正确性、边界行为和回归风险，不是单纯追求覆盖率数字。
3. 测试设计应与模块边界、异常边界、输入输出契约保持一致。

### 6.2 测试分层

项目测试应按层次组织：

1. 单元测试：验证纯逻辑、分支、边界、异常
2. 集成测试：验证模块间协作、数据库、文件系统、外部依赖接入
3. 端到端测试：验证关键业务主链路

### 6.3 可测试性设计要求

1. 核心逻辑必须与外部依赖解耦。
2. 副作用边界必须显式。
3. 依赖必须可替换为 mock、fake、stub。
4. 禁止将主要业务逻辑直接堆入入口函数、脚本主流程或 Web 处理函数。

### 6.4 测试用例要求

1. 每个核心模块至少覆盖：
   - 正常路径
   - 边界条件
   - 异常路径
2. 测试名称必须表达场景与预期结果。
3. 测试必须独立、可重复执行、与顺序无关。
4. 测试不得依赖真实外部服务，除非明确归类为集成测试或端到端测试。

### 6.5 测试数据管理

1. 测试数据必须最小化。
2. 固定输入应产生固定结果。
3. 生产敏感数据不得直接用于测试。
4. 测试对象构造应通过夹具、工厂方法或统一辅助函数管理。
5. 大样本测试数据应分层管理，避免在单元测试中引入重型数据依赖。

### 6.6 覆盖率要求

1. 核心模块必须具备单元测试。
2. 关键链路必须具备集成测试。
3. 覆盖率门槛可作为辅助约束，但不得替代测试有效性评审。
4. 不得以无意义测试堆高覆盖率。

### 6.7 回归与发布前验证

1. 每次变更必须执行与影响范围相匹配的自动化测试。
2. 发布前必须完成关键主链路回归验证。
3. 缺陷修复必须补充回归测试，防止同类问题重复出现。

---

## 7. 配置与依赖管理

### 7.1 配置管理原则
1. 配置项必须定义类型、默认值、是否必填、取值约束。
2. 程序启动阶段必须执行配置校验，并在配置不合法时快速失败。
3. 环境变量、配置文件、密钥管理系统的职责边界必须明确。

### 7.2 环境隔离
1. 开发、测试、生产环境配置必须隔离。
2. 不同环境的数据库、缓存、对象存储、消息系统不得混用。
3. 本地调试配置不得污染生产配置管理。

### 7.3 敏感配置管理

1. 密钥、令牌、密码、证书等敏感配置不得写入代码仓库。
2. 敏感配置必须通过安全渠道注入，例如环境变量或密钥管理系统。
3. 调试输出中不得泄露敏感配置内容。

### 7.4 依赖声明与版本管理

1. 使用 `pyproject.toml` 作为主依赖声明入口。
2. 依赖版本必须可重现。
3. 必须区分运行依赖、开发依赖、测试依赖。
4. 禁止无评估引入来源不明、长期无人维护或高风险依赖。

### 7.5 依赖升级策略

1. 依赖升级必须可审计。
2. 大版本升级前必须评估兼容性影响。
3. 升级后必须执行相应回归测试。
4. 安全漏洞依赖必须建立定期扫描与修复机制。

### 7.6 运行入口与打包

1. 程序入口必须清晰、稳定、可文档化。
2. CLI 参数必须支持帮助说明、类型校验与错误提示。
3. 构建产物必须可复现。
4. 发布方式必须与项目类型匹配，如 wheel、容器镜像、压缩包、任务脚本包等。

---

## 8. 不同项目类型的专项约束

### 8.1 轻量脚本类项目

适用场景：

- 定时任务
- 数据清洗脚本
- 批处理脚本
- 单机自动化流程

专项要求：

1. 优先采用过程式组织与小函数拆分。
2. 主入口必须唯一。
3. 参数解析、配置加载、主流程、结果输出必须分层。
4. 必须支持重复执行。
5. 必须有明确退出码。
6. 必须具备基础日志与配置外置能力。
7. 禁止为简单脚本引入伪分层、伪抽象、过度面向对象包装。

### 8.2 公共库 / SDK 类项目

适用场景：

- 公共工具库
- 内部共享组件
- 算法封装库
- 对外提供调用接口的 SDK

专项要求：

1. API 稳定性优先于内部实现灵活性。
2. 公共接口必须完整类型标注。
3. 公共接口必须有清晰文档。
4. 禁止库内部直接执行进程退出。
5. 禁止在库内部直接打印控制台输出作为主反馈手段。
6. 异常体系必须便于调用方稳定处理。
7. 向后兼容性变更必须明确版本策略。

### 8.3 服务 / 系统类项目

适用场景：

- Web 服务
- API 服务
- 后台任务系统
- 跨模块业务系统

专项要求：

1. 分层结构必须清晰。
2. 请求入口、应用服务、领域逻辑、基础设施边界必须明确。
3. 外部依赖调用必须具备超时控制。
4. 重试、限流、降级、熔断策略应按场景设计。
5. 必须具备健康检查、日志、监控、告警能力。
6. 请求链路必须传递 `request_id` 或 `trace_id`。
7. 错误响应格式必须统一。
8. 审计要求明确的系统必须提供审计日志能力。

### 8.4 数据 / 算法类项目

适用场景：

- 数据处理流水线
- 特征工程
- 模型训练
- 模型评测
- benchmark 数据构建

专项要求：

1. 输入输出 schema 必须显式定义。
2. 数据目录、产物命名、版本管理必须标准化。
3. 关键参数、随机种子、评测配置必须可复现。
4. 实验代码、生产代码、临时分析代码必须分离。
5. 评测流程必须可追踪、可复跑、可审计。
6. 数据质量校验必须纳入主流程，而不是事后补救。
7. 大规模批处理必须定义失败重试、断点续跑、结果一致性策略。

---

## 9. 评审与落地建议

### 9.1 规范执行方式

本规范应通过以下机制落地，而不是停留在文档层面：

1. 模板化项目脚手架
2. CI 门禁
3. Code Review 检查表
4. 发布前检查表
5. 关键模块设计评审

### 9.2 评审重点

评审时应重点检查以下问题：

1. 模块边界是否清晰
2. 接口契约是否完整
3. 异常与错误码是否统一
4. 日志是否可定位问题且无敏感泄露
5. 测试是否覆盖关键边界
6. 配置与依赖是否可控
7. 是否符合项目类型专项约束

### 9.3 不合格的典型表现

以下情况可直接判定为工程质量不合格或存在明显风险：

- 大量硬编码配置
- 缺少类型标注和文档字符串
- 异常被吞掉或静默降级
- 日志重复、混乱、无法定位问题
- 关键流程无测试
- 模块强耦合、循环依赖
- 生产代码与实验代码混放
- 目录结构失控、`utils` 泛滥
- 外部依赖直接写死在核心逻辑中

